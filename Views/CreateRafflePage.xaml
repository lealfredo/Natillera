<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Natillera.ViewModels"
             x:Class="Natillera.Views.CreateRafflePage"
             x:DataType="viewModels:CreateRaffleViewModel"
             xmlns:sys="clr-namespace:System;assembly=netstandard"
             Title="{Binding Title}">
    
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <Label
                Text="Datos de la rifa"
                FontAttributes="Bold"
                FontSize="18" />

            <Entry
                Placeholder="Nombre de la lotería"
                Text="{Binding RaffleWeek.LotteryName}" />

            <Entry
                Placeholder="Código de la semana"
                Text="{Binding WeekCode}"
                IsEnabled="False" />

            <Label Text="🎁 ¿Qué se está rifando?"
                   FontAttributes="Bold" />

            <Entry Placeholder="Ej: $500.000 en efectivo"
                   Text="{Binding RaffleWeek.PrizeDescription}" />

            <Label Text="📅 Fecha del sorteo"
                   FontAttributes="Bold" />

            <DatePicker
                Date="{Binding DrawDate}"
                MinimumDate="{x:Static sys:DateTime.Today}" />

            <Label Text="Precio de la boleta"
                   FontAttributes="Bold" />
            
            <Entry Placeholder="Precio de la boleta"
               Keyboard="Numeric"
               Text="{Binding RaffleWeek.BetPrize}" />

            <Label Text="💰 Premios por acierto"
                   FontAttributes="Bold"
                   Margin="0,10,0,0"/>

            <Entry Placeholder="Premio 2 primeras cifras"
                   Keyboard="Numeric"
                   Text="{Binding RaffleWeek.FirstTwoPrize}" />

            <Entry Placeholder="Premio 2 cifras del medio"
                   Keyboard="Numeric"
                   Text="{Binding RaffleWeek.MiddleTwoPrize}" />

            <Entry Placeholder="Premio 2 últimas cifras"
                   Keyboard="Numeric"
                   Text="{Binding RaffleWeek.LastTwoPrize}" />


            <Label Text="Rifa personal:" FontAttributes="Bold" VerticalOptions="Center"/>
            <CheckBox IsChecked="{Binding RaffleWeek.IsPersonal}" />

            <Button Text="Guardar"
                    Command="{Binding CreateRaffleCommand}"
                    BackgroundColor="#455A64"
                    TextColor="White"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>